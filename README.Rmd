---
output: github_document
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "man/figures/README-",
  out.width = "100%"
)
```

# causaleffects

The goal of `causaleffects` is to estimate some useful causal estimands using different estimators.

## Installation

You can install the development version of `causaleffects` like so:

```{r}
devtools::install_github("Causality-Bites/causaleffects")
```
